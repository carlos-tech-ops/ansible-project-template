ansible-playbook playbooks/main.yml --ask-become-pass
---
- name: Create or overwrite /opt/ops.txt
  ansible.builtin.copy:
    content: "Rewritten by Ansible on {{ ansible_date_time.date }} at {{ ansible_date_time.time }}"
    dest: /opt/ops.txt
    owner: root
    group: root
    mode: '0444'
